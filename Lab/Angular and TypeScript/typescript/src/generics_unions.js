// const window: WindowStates = 'asd'//we can't
var oneWindow = 'open'; //or closed or minimized
var backpack;
backpack.add('simple_object');
var object = backpack.get(); //now object is a string.
console.log('object', object);
